<h2><%= @product.name %></h2>
<p><%= @product.stock %></p>
<p><%= @product.price %></p>

<%= link_to "Delete",
  product_path(@product),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Edit",
  edit_product_path(@product) %>
